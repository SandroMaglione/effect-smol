---
"effect": patch
---

Add an unstable machine prototype with schema-backed tagged events and states, typed handlers, pure transition planning with effect actions, and a scoped actor runtime under `effect/unstable/machine/Machine`.
